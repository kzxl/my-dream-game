---
project_name: mdg
full_name: My Dream Game (MDG)
domain: arpg_game_engine
primary_language: csharp
architecture: pure_csharp_headless_core, clean_architecture, universe_plugin_v4, event_driven
platform: cross_platform (pc, mobile, web)
framework: net8, netstandard2.1
server: aspnetcore_webapi_fixed_tick_30
framework_references:
  agent_option_root: "D:\\Project\\AgentOption"
  rules: ["R_CORE", "R_CS", "R_API", "R_UNIVERSE", "R_EVT", "R_RES", "R_TDD", "R_PERF", "R_STATE", "R_ENG_OS"]
  shortcuts: ["CLEAN", "UNIV", "EVT", "RES", "TDD", "PERF", "STATE", "BLAST", "NORM", "CONS", "PONY", "SOLID", "CTOK"]
key_paths:
  core_root: "src/Mdg.Core/"
  features_root: "src/Mdg.Core/Features/"
  plugins_root: "src/Mdg.Plugins/"
  client_adapter: "src/Mdg.Client.Adapter/"
  server_root: "src/Mdg.Server/"
  tests_root: "tests/"
  docs_design: "docs/design/"
  docs_lore: "docs/lore/"
---

# Project Rule Configuration & Directives

- **Tên dự án:** My Dream Game (MDG)
- **Kiến trúc cốt lõi:** Pure C# Headless Core, Clean Layered Architecture, Universe Plugin (UA v4.0), Event-Driven Architecture.
- **Tiêu chuẩn tích hợp AgentOption:**
  - Quy tắc cốt lõi: `[AgentOption]/rules.json` (`R_CORE`, `R_CS`, `R_API`, `R_UNIVERSE`, `R_EVT`, `R_RES`, `R_TDD`, `R_PERF`, `R_STATE`, `R_ENG_OS`).
  - Kỹ năng & Mẫu thiết kế: Tham chiếu `[AgentOption]/skills/` và `[AgentOption]/architecture/`.

## 📜 Nguyên Tắc Sáng Tạo & Tham Khảo Ý Tưởng (Creative & Originality Guidelines)
- **Không sao chép nguyên mẫu (No Verbatim Copying):** Khi tham khảo ý tưởng, cơ chế từ các nguồn bên ngoài (SAO, PoE, Grim Dawn, Diablo...), TUYỆT ĐỐI KHÔNG copy nguyên văn lore, tên gọi, công thức hay hệ thống vào Core và Design.
- **Chắt lọc & Phá cách (Essence Distillation & Innovation):** Chỉ lấy cảm hứng về mặt trải nghiệm / triết lý gameplay, sau đó tái cấu trúc và thiết kế lại theo hướng độc đáo, đột phá và khác biệt.
- **Bản sắc riêng biệt (Unique World Identity):** Mọi thuật ngữ, lore, tên quái vật, vật phẩm đặc trưng và hiệu ứng kỹ năng phải mang đậm bản sắc thế giới *Aethelis* (Aether, Resonance, Sylvan, Void Sovereign, Celestial Fracture...), tránh trùng lặp hoặc mượn nguyên mẫu từ bên ngoài.

## 🛡️ Invariant Commit Gate (5 Điểm Kiểm Tra Bắt Buộc)
1. **Quyền tự chủ dữ liệu (Universe Plugin Data Sovereignty):** Tuyệt đối không truy vấn chéo dữ liệu giữa các module plugin ngoài cơ chế EventBus/MessageQueue.
2. **Nguyên tắc tối giản (PONY / YAGNI):** Không sinh code thừa hoặc các tầng trừu tượng không cần thiết.
3. **Quét tầm ảnh hưởng (Blast-Radius):** Quét caller (`grep/rg`) trước khi sửa đổi các public/shared symbol.
4. **Không dùng pattern cấm:** Không dùng `BindingSource`, synchronous blocking (`.Result`, `.Wait()`), v.v.
5. **Commit rõ ràng bằng Tiếng Việt:** Đảm bảo toàn vẹn code và commit sau mỗi tính năng hoàn tất.


